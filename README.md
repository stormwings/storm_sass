# Storm SASS
v0.0.1

- Start project

StormSASS it's an Atomic SASS Design System

**First time**:
+  `npm install` ( only the first time )
+  `npm install -g node-sass`

**Start**:
+  `npm run start` 
